# 테이블 설계

* 테이블 비교

```sql
CREATE TABLE fish_info(
    common VARCHAR(20), -- Geroge gay
    spices VARCHAR(20),
    location VARCHAR(30), --Virginia, NY
    weight VARCHAR(20)
);
```

```sql
CREATE TABLE fish_info(
    first_name VARCHAR(20), --George
    last_name VARCHAR(20), --gay
    common VARCHAR(20), -- George gay
    spices VARCHAR(20),
    location VARCHAR(30), -- Virginia
    weight VARCHAR(20),
    state CHAR(2), -- NY
    date DATE
);
```

